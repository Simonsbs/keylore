replicaCount: 2

app:
  environment: staging
  logLevel: info
  bootstrapFromFiles: true

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 70

serviceMonitor:
  enabled: true

networkPolicy:
  enabled: true
